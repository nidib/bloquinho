{
	"name": "@bloquinho",
	"version": "0.9.0",
	"scripts": {
		"lint": "pnpm -r --parallel lint",
		"lint:fix": "pnpm -r --parallel lint:fix",
		"compile": "pnpm -r --parallel compile",
		"build": "pnpm -r --parallel build",
		"precommit": "pnpm lint && pnpm compile && pnpm build"
	},
	"license": "MIT",
	"packageManager": "pnpm@10.12.3",
	"devDependencies": {
		"@biomejs/biome": "^2.0.5"
	},
	"dependencies": {
		"typescript": "^5.8.3"
	}
}
